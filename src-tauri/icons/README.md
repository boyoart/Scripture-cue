# Local icon asset (manual)

To keep pull requests text-only and reviewable, `icon.ico` is intentionally not tracked here.

Before running or bundling the Tauri app, ensure this file exists locally:

- `src-tauri/icons/icon.ico`

`src-tauri/tauri.conf.json` already points to this path.
